#! backend-deployment.yaml
# Generated code, do not edit
apiVersion: apps/v1
kind: Deployment
metadata:
    name: backend
    namespace: microservices-docker-compose
    labels:
        com.docker.compose.project: microservices-docker-compose
        com.docker.compose.service: backend
spec:
    replicas: 1
    selector:
        matchLabels:
            com.docker.compose.project: microservices-docker-compose
            com.docker.compose.service: backend
    strategy:
        type: Recreate
    template:
        metadata:
            labels:
                com.docker.compose.project: microservices-docker-compose
                com.docker.compose.service: backend
                com.docker.compose.network.app-network: "true"
        spec:
            containers:
                - name: backend
                  image: microservices-docker-compose-backend
                  imagePullPolicy: IfNotPresent
                  env:
                    - name: DATABASE_URL
                      value: "postgresql://user:password@db:5432/mydb"
                  ports:
                    - name: backend-5000
                      containerPort: 5000
