#! db-deployment.yaml
# Generated code, do not edit
apiVersion: apps/v1
kind: Deployment
metadata:
    name: db
    namespace: microservices-docker-compose
    labels:
        com.docker.compose.project: microservices-docker-compose
        com.docker.compose.service: db
spec:
    replicas: 1
    selector:
        matchLabels:
            com.docker.compose.project: microservices-docker-compose
            com.docker.compose.service: db
    strategy:
        type: Recreate
    template:
        metadata:
            labels:
                com.docker.compose.project: microservices-docker-compose
                com.docker.compose.service: db
                com.docker.compose.network.app-network: "true"
        spec:
            containers:
                - name: db
                  image: postgres:14
                  imagePullPolicy: IfNotPresent
                  env:
                    - name: POSTGRES_DB
                      value: "mydb"
                    - name: POSTGRES_PASSWORD
                      value: "password"
                    - name: POSTGRES_USER
                      value: "user"
                  ports:
                    - name: db-5432
                      containerPort: 5432
                  volumeMounts:
                    - name: var-lib-postgresql-data
                      mountPath: /var/lib/postgresql/data
            volumes:
                - name: var-lib-postgresql-data
                  persistentVolumeClaim:
                    claimName: db-postgres-data
